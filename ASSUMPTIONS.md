# ASSUMPTIONS — قرارات اتُّخذت أثناء التنفيذ (بدون أسئلة)

## جلسة طبقة المحتوى (main)
1. **البنية الحالية هي الأساس**: لم تُبنَ أنظمة موازية. `src/data/{math,ageGroups,vocab}.js` هي مصادر الحقيقة للمحتوى الحي، والملفات الجديدة (`src/content`, `src/logic`) توسّعها ولا تستبدلها.
2. **الفئات العمرية الأربع باقية** (`3-4/5-6/7-8/9-10`) لأن القوائم والشاشات مبنية عليها. وحدات السن المنفرد (3..10) موجودة كبيانات مرتبة في `src/content/math/units.js`، و`ageMinOf` يمثّل الفئة بأصغر سن فيها (محافظ: اختيارات أقل وأسئلة أقصر).
3. **النصوص العربية القديمة لا تُمس**: مقاطع الصوت المولّدة (`assets/audio`) نُطقت بالنصوص الحالية (عامية مصرية)، فأي تغيير في `ar` كان سيكسر تطابق الصوت — لهذا بقيت الصياغات القديمة، والفصحى المبسطة للكلمات الجديدة فقط.
4. **الاختيارات**: 2/3/4/4 للأعمار (7-8 → 4 لو توفرت مشتتات كافية، وإلا 3). الثنائيات الحتمية (زوجي/فردي) تبقى اختيارين دائمًا.
5. **ملف واحد للبروفايل**: التطبيق أحادي البروفايل؛ حقول التقدم الجديدة (`units/boxes/daily`) داخل نفس كائن التقدم مع دمج آمن يحافظ على البيانات القديمة. لا مفاتيح لكل طفل (لا يوجد تعدد بروفايلات في الواجهة).
6. **الاختبارات بدون حزم جديدة**: استُخدم `node:test` الأصلي (ملفات `.cjs` + `require`) بدل Jest لتفادي أي تغيير في الاعتماديات. تحذير `MODULE_TYPELESS_PACKAGE_JSON` تجميلي فقط ولا يمنع النجاح.
7. **الأرقام المنطوقة**: مولّد الأرقام يرسل الأرقام كأرقام (`"45"`) مع توجيه اللهجة المصرية، بدل كتابة 121 كلمة عددية.
8. **`level_offset` للأهل**: لا يوجد إعداد له في الواجهة، فالدالة `shiftBounds` موجودة والربط مؤجل (Needs UI decision).
9. **الكلمات المجردة**: لم تُدخل أي كلمة مجردة للصغار؛ كل المضاف من المحسوسات.
10. **شراب/Socks**: بقيت الصياغة المصرية (شراب) بدل جورب/Sock لاتساق الصوت المولّد.

## جلسة وضع المعلم (teacher-mode)
11. **فرع منفصل بلا نشر**: كل الشغل على teacher-mode، كوميتات محلية فقط.
12. **المشاهد حرفية لا مولّدة**: كُتبت JSON يدويًا لكل وحدة لضمان جودة الشرح (بدل مولّدات عامة قد تخرج جملًا ركيكة).
13. **الأرقام تُنطق عربية دائمًا** في العدّ والمعادلات (واحد، اثنان...) مع ظهور أرقامها؛ الإنجليزية تظهر مكتوبة وتُنطق في showNumeral فقط.
14. **التتبع بالإصبع أُجّل**: يحتاج معايرة مسار معقدة — مسجّل في REPORT (لم يُنفّذ مؤقتًا).
15. **Guided بلا نجوم** ويستخدم AskStep الموحد؛ الإجابة الصحيحة تلمع بعد أول غلطة (حسب مواصفات الجلسة).
16. **التخطي**: أول زيارة للدرس بلا زر تخطي (إكمال أو التالي خطوة بخطوة)؛ الدروس المنجزة سابقًا تُظهره.
