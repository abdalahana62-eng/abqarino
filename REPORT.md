# REPORT — طبقة المحتوى والتعليم (بدون أي تغيير بصري)

## 1. اللي اتنفّذ

### 1.1 محتوى الحساب (`src/data/math.js` + `src/content/math/units.js`)
- **13 موضوعًا مولّدًا برمجيًا**: counting/addition/subtraction/multiplication/division/fractions/wordProblems (كانت موجودة) + **compare/nextnum/missing/shapes/patterns/evenodd** (جديدة).
- كل مولّد يقبل `opts = { rng, unit, count, age }` مع الحفاظ على التوقيع القديم.
- **Seed ثابت**: `createRng(seed)` + خلط Fisher-Yates بالـ RNG المحقون — نفس الـ seed يعطي نفس السؤال (مُختبر).
- **اختيارات معقولة**: ±1/±2 للصغار، +أخطاء شائعة للكبار (±10 نسيان الحمل، عكس الأرقام)، فريدة، تشمل الإجابة، بلا سوالب تحت سن 9 (مُختبر على 208 أسئلة).
- **طرح بلا سوالب** (b<a دائمًا، وفرع `noBorrow`)، **قسمة بلا باقي** (`a=b*ans`)، جمع `noCarry`، جداول محددة (`tables`)، مسائل بخطوتين (`twoStep`) للكبار.
- **عدد اختيارات حسب السن**: 2 (3-4) / 3 (5-6) / 4 (7-10) — تُمرر كمصفوفة فتعرضها الشاشات كما هي.
- **عدد أسئلة حسب السن**: 6 (3-5) / 8 (6-10) عبر `roundsForAge` (سطرا منطق فقط في شاشتي اللعب).
- **الوحدات**: `MATH_UNITS` — 54 وحدة مرتبة للأعمار 3..10 (محسوس→رمز→مجرد، فكرة واحدة، rounds/choices لكل وحدة) مع `supported`، ودوال `unitsForAge/supportedUnitsForAge`.
- القوائم ظهرت فيها المواضيع الجديدة تلقائيًا (بيانات فقط): 3-4 ‏(عدّ/أشكال/مقارنة/جمع)، 5-6 ‏(+التالي/الناقص)، 7-8 ‏(+أنماط/زوجي)، 9-10 ‏(+زوجي/الناقص).

### 1.2 الكلمات (`src/data/vocab.js` + `src/content/words/`)
- **~200 كلمة**: كل قوائم المواصفات أُدخلت (حيوانات 30، ألوان 12، أكل 16، جسم 13، مدرسة 13، طبيعة 14، أفعال 21، جمل 12، ملابس 10، مواصلات 10، وظائف 10، مشاعر 10، بيت 10، عكس 8، أيام 7، شهور 12، أماكن 8، فضاء 4، رياضة 4) + مواضيع جديدة للقوائم: home/opposites/days/months/places/space/sports.
- كل كلمة: `{ar, en, emoji}` + `min_age` في `meta.js` + **تشكيل كامل لأعمار 3-7** في `diacritics.js` (يُعرض المشكّل ≤7 سنوات عبر `wordDisplay`).
- **بانِي الأسئلة** `src/logic/wordQuestions.js`: المشتتات من نفس الفئة مع تفضيل اختلاف الحرف الأول (بقرة←حصان)، مربوط في شاشة الكلمات (منطق فقط).

### 1.3 المنطق (`src/logic/`)
- `difficulty.js`: `ageMinOf/roundsForAge/choicesForAge/adapt (3 غلط↓/5 صح سريع↑ بحد ±2)/shiftBounds/lessonComplete (80%)/starsFor (60/80/95)`.
- `spacedRepetition.js`: صناديق Leitner الخمسة (1/3/7/14/30 يوم)، صح→التالي وغلط→1، `getDueItems` (3-5 عناصر، الأدنى أولًا).
- `progress.js`: `recordLesson` (النجوم تزيد فقط عند التحسن، بلا تحوير)، `recordReviewItem`، `addDailyTime/isLimitReached` (15 دقيقة)، `unlockedUnits` (فتح تسلسلي).
- `storage.js`: حقول `units/boxes/daily` بدمج آمن + دوال حفظها (نفس البروفايل الواحد).

### 1.4 الصوت والنصوص
- `src/content/phrases.json`: أسئلة/مدح/محاولة/صدى إنجليزي (`{en}! {ar} تساوي {en}`)/نصوص المعلم بقوالب `{name}/{score}/{total}` — **وبلا أي كلمة ممنوعة** (مُختبر آليًا).
- `speech.js`/`speech.web.js`: نصوص المعلم تُقرأ من `phrases.json` (منطق فقط، نفس الأصوات).
- `voice.js`: دالة `playWord(word)` — تسجيلات `assets/audio/ar|en/{id}.mp3` أولًا، ثم مقاطع wav المجمّعة، ثم TTS الجهاز (عربي→إنجليزي)، ولا تكسر التطبيق أبدًا. شرح شفهي للمواضيع الستة الجديدة.

### 1.5 الاختبارات
- `src/logic/__tests__/` (4 ملفات، node:test الأصلي، صفر حزم جديدة): **23/23 ناجحة** — مولدات (إجابة/تفرد/لا سوالب/ثبات Seed/وحدات)، صعوبة، كلمات (تفرد المشتتات/التشكيل/سلامة البيانات)، مراجعة وتقدم، ومسح الكلمات الممنوعة.
- التشغيل: `node --test 'src/logic/__tests__/math.test.cjs' 'src/logic/__tests__/difficulty.test.cjs' 'src/logic/__tests__/words.test.cjs' 'src/logic/__tests__/learning.test.cjs'`
- الحزم: `npx expo export --platform web --output-dir dist` نجح (التطبيق يتحزم ويفتح).

## 2. Needs UI decision (لم تُنفذ عمدًا — تحتاج شاشات/عناصر جديدة)
1. **تقدّم الوحدات**: بيانات `MATH_UNITS` + `unlockedUnits` جاهزة، لكن لا توجد شاشة خريطة دروس/قفل — القوائم الحالية تعرض المواضيع لا الوحدات المرتبة.
2. **شاشة/زر مراجعة**: `getDueItems` جاهزة ولا يوجد مكان يعرضها.
3. **رسالة انتهاء الوقت اليومي**: `isLimitReached` جاهزة ولا توجد شاشة تعرضها.
4. **`level_offset` للأهل**: `shiftBounds` جاهزة ولا يوجد إعداد في ParentGate.
5. **مراحل الكلمة** (اختيار الصورة المستقل، ترتيب الحروف 7+، كتابة 9+): المكونات تدعم (صورة→اسم) والنطق فقط.
6. **وحدات `supported:false`**: الساعة (كاملة/نص/ربع)، النقود، العشريات، القياس، المحيط/المساحة — تحتاج صورًا بصرية (ساعة، عملات، أشكال هندسية بمقاسات).
7. **تفاعل إدخال رقم** (7-10): المكونات تدعم ضغطًا وسحبًا فقط.
8. **صدى الإنجليزي** `{en}! {ar} تساوي {en}` كنطق مركّب: يحتاج تسجيلات (انظر Missing assets).

## 3. Missing assets
- **صور**: لا توجد صور مطلوبة — كل الكلمات الجديدة لها Emoji (بما فيها القمر/الباب/المفتاح/الساعة، و`🪑` للطاولة و`🐧` للقصير و`🐆` للسريع و`🐭` للصغير كبدائل مرسومة بوضوح مختلف).
- **أصوات**: `assets/audio/` فيه 28 مقطعًا فقط. الناقص (يُستخدم TTS الجهاز تلقائيًا لحين توليده): كل كلمات `vocab.js` الجديدة، الأرقام n8,n10+ ، `plus/minus/times/divide/eqwhat/howmany/fracq`، تعليمات السحب الست، `bye_good/bye_try/must_age/lesson_intro/repeat_after/say_loud/good_speak`، وتسجيلات `ar|en/{id}.mp3` المستقبلية.

## 4. Findings (للإبلاغ فقط — لم يُصلَّح شيء)
- `npx expo-doctor`: 18/21. الفشل الثلاثة سابق ومقصود عدم لمسه: (1) `app.json` فيه حقل `main` خارج السكيما، (2) `expo-av` غير مُ maintained حسب الدليل (بديله `expo-audio` — **لم يُستبدل** احترامًا لقاعدة عدم الترقية)، (3) نسخ `expo-haptics/speech/status-bar` و`react/react-native` أقدم من SDK 57 المطلوبة.
- تحذير `MODULE_TYPELESS_PACKAGE_JSON` عند تشغيل الاختبارات تجميلي (إعادة تفسير ESM) ولا يمنع النجاح؛ لم يُضف `"type": "module"` لتفادي كسر Metro.
- `math.js` سطر الاستيراد أصبح `'./vocab.js'` (امتداد صريح) لتوافق node — Metro يقبله ولا يغيّر السلوك.

## 5. فحص عدم تغيير التصميم
`git diff 6b194f9..HEAD --stat`: كل الملفات جديدة (content/logic/tests/docs) أو بيانات/منطق (`math/vocab/ageGroups/phrases/speech/voice/storage`)، **ولا ملف ثيم/ستايل/صورة/`app.json`**. تعديلا الشاشتين (`MathPlayScreen` 8 أسطر، `VocabPlayScreen` ~12 سطرًا): استيرادات + `ROUND` المحسوب + بانِي الاختيارات — **لا JSX ولا StyleSheet** (رُوجعت سطرًا بسطر).

## 6. التشغيل والاختبار لكل سن
1. `npm install` (أول مرة) ثم `npx expo start --web`.
2. الشاشة الأولى → ParentGate: اختر فئة (3-4/5-6/7-8/9-10) → Home.
3. الحساب → ادخل كل موضوع في القائمة (الجديدة: مقارنة/التالي/الناقص/أشكال/أنماط/زوجي) والعب جولة (6 أسئلة لـ 3-5، و8 لغيرهم؛ اختيارين لـ 3-4).
4. الكلمات → كل موضوع (الجديدة: بيت/عكس/أيام/شهور/أماكن/فضاء/رياضة) بوضعي الاختبار والنطق.
5. الاختبارات: أمر `node --test` أعلاه (23/23).
6. التقدم: النجوم والوحدات في `@abqarino/progress` (افحصها بـ `AsyncStorage` أو ذاكرة الويب).
